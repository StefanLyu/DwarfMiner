const config = {
    database: {
        DATABASE: 'dwarf_miner',
        PORT: 3306,
        HOST: 'localhost',
        USER: 'dwarf_miner',
        PASSWORD: 'dwarf_miner'
    }
}

module.exports = config;